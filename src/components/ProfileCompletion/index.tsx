import React from 'react';
import { IonSpinner, IonCard, IonContent, IonGrid, IonCardTitle, IonCardContent } from '@ionic/react';

const ProfileCompletion: React.FC = () => {
  return (
    <IonContent className={"profilecompletion"}>
      {/*-- Default ProfileCompletion --*/}
      <IonCard>
        <IonCardTitle>Profile Completion</IonCardTitle>
        <IonCardContent>tets</IonCardContent>
      </IonCard>
    </IonContent>
  )
};

export default ProfileCompletion;
