// import * as createAccountSocial from '../fetchapi';
// const chai = require('chai');
// const sinon = require('sinon');
// const sinonChai = require('sinon-chai');
const assertSettings = require('assert');

describe('Setting Functionalities', () => {
  it('Manage account works fine', () => {
    assertSettings.equal(true, true);
  });
  it('Settings body loads and renders fine', () => {
    assertSettings.equal(true, true);
  });

  it('Contact features works', () => {
    assertSettings.equal(true, true);
  });

  it('Settings header works', () => {
    assertSettings.equal(true, true);
  });

  it('Help section works', () => {
    assertSettings.equal(true, true);
  });

  it('Navbar Works', () => {
    assertSettings.equal(true, true);
  });

  it('Report feature works', () => {
    assertSettings.equal(true, true);
  });

  it('Payment and Subscriptions works', () => {
    assertSettings.equal(true, true);
  });
});
