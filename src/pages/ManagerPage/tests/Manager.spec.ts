// import * as createAccountSocial from '../fetchapi';
// const chai = require('chai');
// const sinon = require('sinon');
// const sinonChai = require('sinon-chai');
const assertManager = require('assert');

describe('Manager Page Functionalities', () => {
  it('Email compare and update works', () => {
    assertManager.equal(true, true);
  });

  it('Profile editing works', () => {
    assertManager.equal(true, true);
  });

  it('Public fields updation and deletion works', () => {
    assertManager.equal(true, true);
  });

  it('Modal card works', () => {
    assertManager.equal(true, true);
  });
});
