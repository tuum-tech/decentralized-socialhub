// import * as createAccountSocial from '../fetchapi';
// const chai = require('chai');
// const sinon = require('sinon');
// const sinonChai = require('sinon-chai');
const assert = require('assert');

describe('Dashboard Functionalities', () => {
  it('Should Load dashboard content', () => {
    assert.equal(true, true);
  });
  it('Should Load dashboard header', () => {
    assert.equal(true, true);
  });

  it('Links work fine', () => {
    assert.equal(true, true);
  });

  it('Onboarding works', () => {
    assert.equal(true, true);
  });

  it('Should publish label', () => {
    assert.equal(true, true);
  });

  it('Should Start Services', () => {
    assert.equal(true, true);
  });

  it('Tutorial Should work', () => {
    assert.equal(true, true);
  });
});
